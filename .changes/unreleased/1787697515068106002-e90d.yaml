kind: 'Fixed'
body: 'pointed the pull request checklist at `make lint`, `make test` and `make sast` instead of `go test`, which skips the suites behind the `unit` build tag and runs neither the linter nor the SAST suite'
time: '2026-08-25T22:38:35.06802091Z'
